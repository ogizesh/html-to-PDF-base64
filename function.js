window.function = function(html) {
    let result = {
        success: true,
        message: "HTML received: " + html.value,
        base64String: "This is a placeholder"
    };

    return result;
};
